	// one task, check errors if im freeing correctly
	// incorrect behavior echo haha >> output >> out
	//error_malloc free ok_malloc free could be the same
	//still have malloc issues 
	// use just one env_array in main